import { LinkNew } from "@/src/components/Link"

export default function Page404(){
    return(
        <div>
          <h1>Você se perdeu na net, essa página não existe</h1>
          <LinkNew label="Ir para a Home Page" url="/" />
      </div>
    )
}